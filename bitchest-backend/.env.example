APP_NAME=Bitchest
APP_ENV=local
APP_KEY=base64:/8zr515aMlKHSGk3jeRRYOXOaQD520Dr/ILQJwKQJmk=
APP_DEBUG=true
APP_URL=http://localhost

LOG_CHANNEL=stack
LOG_LEVEL=debug
SUPPORT_BOT_URL=http://127.0.0.1:8001
# -----------------------------
# DATABASE - XAMPP
# -----------------------------
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=bd_bitchest
DB_USERNAME=root
DB_PASSWORD=

# -----------------------------
# QUEUE / SESSION
# -----------------------------
BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

# -----------------------------
# REDIS (pas utilisé mais laissé par défaut)
# -----------------------------
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# -----------------------------
# MAIL CONFIG (GMAIL SMTP)
# -----------------------------
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=shedlylaaddhiby@gmail.com
MAIL_PASSWORD="cuix rene dysl uofn"
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="shedlylaaddhiby@gmail.com"
MAIL_FROM_NAME="Bitchest Wallet"

# -----------------------------
# AWS (non utilisé)
# -----------------------------
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# -----------------------------
# PUSHER (non utilisé)
# -----------------------------
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# -----------------------------
# Support bot (FastAPI — dossier /bot du projet)
# -----------------------------
SUPPORT_BOT_URL=http://127.0.0.1:8001

# -----------------------------
# VITE
# -----------------------------
VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"
